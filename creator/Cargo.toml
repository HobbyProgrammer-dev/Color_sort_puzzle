[package]
name = "creator"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.4.0"
game = { path = "../game" }
rand = "0.10.0"
rand_chacha = "0.10.0"
serde = { version = "1.0.228", features = ["derive"] }
sorter = { version = "0.1.0", path = "../sorter" }
